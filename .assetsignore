# Cloudflare serves everything in this repo as the website — except the paths
# below. These are the dispatch bot, its workflow, and config/docs, which should
# run on GitHub but never be published to theshadowhook.com.
# NOTE: this file must be named exactly ".assetsignore" (leading dot, no .txt).
.github/
bot/
wrangler.toml
README.txt
README.md
