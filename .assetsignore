# Cloudflare serves everything in this repo as the website — except the paths
# below. These are the bots, the edge-function SOURCE, the workflows, and
# config/docs: they run on GitHub / Supabase but must NEVER be published to
# theshadowhook.com. (The deployed edge functions live on Supabase's own infra,
# so ignoring the source here does not affect them at all.)
# NOTE: this file must be named exactly ".assetsignore" (leading dot, no .txt).
.github/
bot/
hall-line-worker/     # #aug12 — WAS EXPOSED: leftoff.js & parse.js were served publicly
supabase/             # #aug12 — WAS EXPOSED: edge-function source (index.ts) was served publicly
wrangler.toml
README.txt
README.md
